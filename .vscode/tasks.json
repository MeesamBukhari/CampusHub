{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "pip install",
      "command": "pip",
      "args": ["install", "-r", "requirements.txt"],
      "options": {
        "cwd": "${workspaceFolder}/backend"
      },
      "presentation": {
        "reveal": "silent"
      }
    }
  ]
}
